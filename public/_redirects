# Cloudflare Pages redirect rules.
#
# Rewrite /sitemap.xml → the sitemap edge function. The :200 forces a 200
# response (proxy/rewrite) instead of a 30x redirect, so search engines
# read the XML directly from this URL.
/sitemap.xml https://kcukkqnkhvhphfdebcuh.supabase.co/functions/v1/sitemap 200

# SPA fallback: client-side router handles every other path.
/* /index.html 200
